const burger = document.querySelectorAll(".toggle-burger")
console.log(burger[0]);

const menu = document.querySelectorAll(".nav");
console.log(menu);

const container = document.getElementById("container");
console.log(container);